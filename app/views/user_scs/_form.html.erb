<%= form_for(@user_sc) do |f| %>
  <% if @user_sc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_sc.errors.count, "error") %> prohibited this user_sc from being saved:</h2>

      <ul>
      <% @user_sc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
